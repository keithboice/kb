default: ["watch", "writefile"]

new:
    description: "Generates new set of repository assets from run"
    tasks:
        - "writefile:readme"

dev:
    description: "Watches relevant file types and directories to build changes to code in real-time"
    tasks:
        - "watch"
